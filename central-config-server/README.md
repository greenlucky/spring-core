**CENTRAL CONFIG SERVER**
------------------------------------
Using _`Spring Cloud Config Server`_, _`Spring Cloud Starter Config`_ and git to store configuration file. But _`/refresh`_ to reload the config file is not working right now.